este error debe ser específicamente de MI instalación de VISUAL STUDIO pero bueno, les aviso chicas
que tienen que tener sí o sí NET5 (no otra versión) y ejecutar en powershell sobre las carpetas normales
y tests los siguientes comandos:

dotnet restore
dotnet run


Estos son útiles para, en vez de ejecutar el csproj en VS, ejecutarlo desde powershell. Si les llega a
saltar más errores nos veremos en la mesa de Diciembre uwu